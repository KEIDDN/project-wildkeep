# Audio drop-in folder

Music is **not** read from here any more: it lives in `MUSIC/` at the project
root (see the README's Audio section).

Sound effects: list a file per id in `SFX_FILES` (`src/data/audio.ts`) and put
it in `sfx/`. Ids without a file use the built-in synthesized sound.
